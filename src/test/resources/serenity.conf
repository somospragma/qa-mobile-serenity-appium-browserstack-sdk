browserstack.user="${browserstack.userName}" # recibido desde comando de ejecución
browserstack.key="${browserstack.accessKey}" # recibido desde comando de ejecución

serenity{
    project.name=serenitybdd mobile
    step.delay=1000
    resized.image.width=600

    take.screenshots=AFTER_EACH_STEP
    logging=VERBOSE
    restart.browser.for.each=NEVER
    timeout=1000
    report.encoding=utf8
}
